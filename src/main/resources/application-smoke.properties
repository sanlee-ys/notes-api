# Active only under the 'smoke' profile (SPRING_PROFILES_ACTIVE=smoke).
# Run the app on 8081 so it doesn't collide with kafka-ui, which is published on
# host port 8080. (The smoke test only needs you to watch kafka-ui; the app's own
# port is incidental here.)
server.port=8081
